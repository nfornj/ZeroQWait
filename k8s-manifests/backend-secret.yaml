# SECURITY NOTICE: This file is a template. Real values are NEVER committed to Git.
# Before first deploy on a new cluster, create a local copy with real values:
#   cp k8s-manifests/backend-secret.yaml k8s-manifests/backend-secret.local.yaml
#   # Edit backend-secret.local.yaml with real credentials, then:
#   kubectl apply -f k8s-manifests/backend-secret.local.yaml
# The deploy-prod.sh script will error if this secret is not already present in the cluster.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: zeroqwait
type: Opaque
stringData:
  DB_USER: "REPLACE_ME"
  DB_PASSWORD: "REPLACE_ME"
  SECRET_KEY: "REPLACE_ME_min_32_random_chars"
  REDIS_PASSWORD: "REPLACE_ME"
  # NVIDIA NIM API key — obtain from build.nvidia.com
  NVIDIA_API_KEY: "REPLACE_ME"
  # Telegram Bot token — obtain from @BotFather on Telegram
  TELEGRAM_BOT_TOKEN: "REPLACE_ME"
  # AWS SES + SNS — IAM user with ses:SendEmail and sns:Publish permissions
  AWS_ACCESS_KEY_ID: "REPLACE_ME"
  AWS_SECRET_ACCESS_KEY: "REPLACE_ME"
  # Infisical Universal Auth machine identity
  INFISICAL_URL: "http://infisical-svc.infisical.svc.cluster.local:8080"
  INFISICAL_PROJECT_ID: "REPLACE_ME"
  INFISICAL_ENV: "prod"
  INFISICAL_CLIENT_ID: "REPLACE_ME"
  INFISICAL_CLIENT_SECRET: "REPLACE_ME"
  INFISICAL_SECRET_PATH: "/"
  # SuperTokens Core API key; connection URI is in backend-config
  SUPERTOKENS_API_KEY: "REPLACE_ME"
  # Backblaze B2 storage
  B2_ENDPOINT: "REPLACE_ME"
  B2_KEY_ID: "REPLACE_ME"
  B2_APP_KEY: "REPLACE_ME"
  B2_BUCKET_NAME: "REPLACE_ME"
  # Brevo transactional email
  BREVO_API_KEY: "REPLACE_ME"
  BREVO_SENDER_EMAIL: "notifications@zeroqwait.com"
  BREVO_SENDER_NAME: "ZeroQWait"
  # Telnyx transactional SMS
  TELNYX_API_KEY: "REPLACE_ME"
  TELNYX_FROM_NUMBER: "REPLACE_ME"
  TELNYX_MESSAGING_PROFILE_ID: ""
