apiVersion: v1
kind: Service
metadata:
  name: asr-service
  namespace: zeroqwait
spec:
  selector:
    app: asr-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
