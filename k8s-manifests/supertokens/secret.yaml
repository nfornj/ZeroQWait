# SECURITY NOTICE: This file is a template. Apply real values manually on first deploy.
apiVersion: v1
kind: Secret
metadata:
  name: supertokens-secret
  namespace: zeroqwait
type: Opaque
stringData:
  POSTGRESQL_CONNECTION_URI: "postgresql://REPLACE_ME_USER:REPLACE_ME_PASSWORD@postgres.zeroqwait.svc.cluster.local:5432/supertokens"
  API_KEYS: "REPLACE_ME"
