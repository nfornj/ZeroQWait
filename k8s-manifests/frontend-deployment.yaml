apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: zeroqwait
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: node:18-alpine
        workingDir: /app
        env:
        - name: NODE_OPTIONS
          value: "--max-old-space-size=1536 --openssl-legacy-provider"
        - name: REACT_APP_API_URL
          value: "/api"
        command: ["/bin/sh", "-c"]
        args:
          - |
            echo "Starting deployment process..."
            
            # Use existing node_modules if possible to speed up restart
            if [ -d "node_modules" ]; then
              echo "Found existing node_modules, attempting to use them..."
            else
              echo "Installing dependencies..."
              npm install --legacy-peer-deps
            fi
            
            echo "Building frontend..."
            # Capture build output to log
            if npm run build; then
              echo "Build command finished successfully."
            else
              echo "❌ Build command FAILED!"
              exit 1
            fi
            
            if [ -d "build" ] && [ -f "build/index.html" ]; then
              echo "✅ Build verification successful: index.html found."
              echo "Starting server on port 3000..."
              npx serve -s build -l 3000
            else
              echo "❌ Build verification FAILED: No index.html found in /app/build"
              echo "Listing /app directory:"
              ls -la /app
              if [ -d "/app/build" ]; then
                 echo "Listing /app/build directory:"
                 ls -la /app/build
              fi
              exit 1
            fi
        ports:
        - containerPort: 3000
        volumeMounts:
        - name: app-code
          mountPath: /app
        resources:
          requests:
            memory: "2Gi"
            cpu: "1000m"
          limits:
            memory: "4Gi"
            cpu: "2000m"
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 300
          periodSeconds: 20
          timeoutSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 180
          periodSeconds: 5
      volumes:
      - name: app-code
        hostPath:
          path: /home/neekrishrichu/apps/zeroqwait/frontend
          type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: zeroqwait
spec:
  selector:
    app: frontend
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30001
